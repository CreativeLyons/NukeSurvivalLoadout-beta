"""NSL boot layer - version gate, self-recovery, sequence.

Submodules import ``nuke`` at module top, so this package does not
auto-import them. Doing so would fail outside a Nuke runtime. Import
each submodule you need explicitly.
"""
