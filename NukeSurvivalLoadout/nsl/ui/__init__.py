"""NSL UI layer - PySide widgets composing the Loadout Panel.

Submodules pull Qt in through ``nsl.compat``, so this package does not
auto-import them. The headless boot path forbids Qt. Import each widget
directly, like ``from nsl.ui.pill import Pill``.
"""
